spec:
  alerts:
  - rule: DEPLOYMENT_FAILED
  - rule: DOMAIN_FAILED
  features:
  - buildpack-stack=ubuntu-22
  ingress:
    rules:
    - component:
        name: button-deploy-test-nginx
      match:
        path:
          prefix: /
    - component:
        name: service
      match:
        path:
          prefix: /service
  name: hammerhead-app
  region: lon
  services:
  - dockerfile_path: Dockerfile
    github:
      branch: nginx
      repo: jkpe/button-deploy-test
    http_port: 80
    instance_count: 1
    instance_size_slug: professional-xs
    name: button-deploy-test-nginx
    source_dir: nginx
  - dockerfile_path: Dockerfile
    github:
      branch: nginx
      repo: jkpe/button-deploy-test
    http_port: 80
    instance_count: 1
    instance_size_slug: professional-xs
    name: service
    source_dir: nginx-1
  